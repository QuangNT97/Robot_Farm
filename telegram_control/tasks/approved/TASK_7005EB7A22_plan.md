# TASK_7005EB7A22 - Display OpenCV camera preview in grayscale

## Request
sửa code opencv_camera.py hiển thị ảnh gray

## Plan
1. Update Project_Farm_Here/opencv_camera.py to describe grayscale preview.
2. Convert each captured BGR frame to grayscale using cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY).
3. Display the grayscale frame with cv2.imshow while keeping existing camera options and quit keys unchanged.

## Files
- `Project_Farm_Here/opencv_camera.py`: Convert camera frames from BGR color to grayscale before displaying them.

## Summary
opencv_camera.py will show the webcam preview in grayscale instead of color. No files will be written until the user approves this proposed change.
